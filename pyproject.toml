[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dfm-mkc-cosmology"
version = "0.1.0"
description = "DFM-MKC cosmology module (URF-aligned)"
requires-python = ">=3.8"
dependencies = [
  "numpy"
]

[tool.setuptools]
packages = ["dfm_mkc"]

