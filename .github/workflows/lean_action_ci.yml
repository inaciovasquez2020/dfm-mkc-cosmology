name: Lean CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Lean only (no build)
        uses: leanprover/lean-action@v1
        with:
          auto-config: true
          build: none
          test: none
          lint: false
          mk_all-check: false
          use-mathlib-cache: auto
          use-github-cache: true

