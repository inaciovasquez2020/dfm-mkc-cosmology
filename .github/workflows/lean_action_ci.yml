name: Lean CI (disabled)

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lean:
    runs-on: ubuntu-latest
    steps:
      - name: Lean not enabled yet
        run: echo "Lean CI intentionally disabled until a Lake project is added."
  workflow_dispatch:
