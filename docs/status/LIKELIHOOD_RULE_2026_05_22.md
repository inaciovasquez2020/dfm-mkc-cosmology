# Likelihood rule

Status: `LIKELIHOOD_RULE_SUPPLIED_FORMAL_ONLY_NOT_EXECUTED`

Required object filled:

- `LIKELIHOOD_RULE`

Root blocker removed:

- `LIKELIHOOD_RULE_NOT_SUPPLIED`

New root blocker:

- `LAMBDA_CDM_BASELINE_VECTOR_NOT_SUPPLIED`

Check result:

- `PASS_FORMAL_RULE_ONLY`

Boundary:

- supplies a formal Gaussian chi-square likelihood rule
- does not execute the likelihood rule
- does not bind empirical data values
- does not bind a Lambda-CDM baseline vector
- does not compute model-selection statistics
- does not supply empirical evidence

Does not prove:

- DFM-MKC
- Lambda-CDM failure
- dark-energy resolution
- dark-matter resolution
- Nobel-level physical discovery
- any Clay problem

Next missing objects:

- `LAMBDA_CDM_BASELINE_VECTOR`
- `INDEPENDENT_EMPIRICAL_VALIDATION`
